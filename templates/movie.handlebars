<article class="movie">
  <a href="/filmer">Tillbaka till filmer</a>
  <h1>{{movie.title}}</h1>
  <div class="intro">
    {{{movie.intro}}}
  </div>
  {{#if movie.image}}
    <img src="{{movie.image.url}}" />
  {{/if}}
</article>